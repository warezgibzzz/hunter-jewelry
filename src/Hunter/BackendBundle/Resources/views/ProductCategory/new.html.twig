{% extends 'HunterBackendBundle:layout:base.html.twig' %}

{% block page %}
    <h1>Product Category creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('hunter_backend_product_category_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
