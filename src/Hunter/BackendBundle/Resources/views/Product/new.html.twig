{% extends 'HunterBackendBundle:layout:base.html.twig' %}

{% block page %}
    <h1>
        {{ 'backend.products.pages.create'|trans }}
        <span class="btn-group pull-right">
            <a href="{{ path('hunter_backend_product_index') }}" class="btn btn-info">{{ 'backend.layout.labels.to_list'|trans }}</a>
        </span>
    </h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="{{ 'backend.layout.labels.save'|trans }}" class="btn btn-success"/>
    {{ form_end(form) }}
{% endblock %}
